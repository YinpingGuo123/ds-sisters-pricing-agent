"""Multi-agent quotation workflow (course MVP).

Note on naming: ``pricing_agent`` is the package for the *whole* workflow, which
contains three agent nodes. The pricing agent specifically lives in
``pricing_agent/agents/pricing.py``.
"""
